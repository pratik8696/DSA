#include <iostream>
using namespace std;

int check(string s1,string s2)
{
    int hash[300];
    fill(hash,hash+300,0);
    for (int i=0;i<s1.length()-1;i++)
    {
        
    }

}

int main()
{
    string s1,s2;
    cin>>s1>>s2;

    if(s1.length()==s2.length())
    {
        return 0;
    }

    check(s1,s2);
    return 0;
}