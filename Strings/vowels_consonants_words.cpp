#include <iostream>
using namespace std;

int vowels(string sentence)
{
    for (int i = 0; i < count; i++)
    {
        
    }
    
}
int main()
{
    string sentence;
    getline(cin,sentence);
    vowels(sentence);
    return 0;
}